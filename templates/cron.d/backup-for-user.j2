#!/bin/bash
# m     h       dom     mon     dow     user    command
{{ backup_minute }}     *       *       *       *       {{ backup_client_local_user|default(backup_client_user) }}  "{{ bin_dir|default('/usr/local/bin') }}/backup-for-{{ backup_client_user }}-cronjob"
