#!/bin/bash
# m     h       dom     mon     dow     user    command
{{ backup_minute }}     *       *       *       *       {{ backup_client_user }}  "/usr/local/bin/backup-for-{{ backup_client_user }}-cronjob"
